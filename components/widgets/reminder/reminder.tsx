import withWidget from '@/components/widgets/with-widget';
import ReminderWidget from '@/components/widgets/reminder/reminder-widget';

const Reminder = withWidget(null, ReminderWidget, null);

export default Reminder;
